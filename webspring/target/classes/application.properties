# JDBC URL do banco de dados.
spring.datasource.url=jdbc:postgresql://localhost:5432/webspring_db
# Login username do banco de dados.
spring.datasource.username=postgres
spring.datasource.password=postgres01

# JPA (JpaBaseConfiguration, HibernateJpaAutoConfiguration)

# Modo DDL. Este � na verdade um atalho para a propriedade "hibernate.hbm2ddl.auto". 
# O padr�o � "criar-descartar" ao usar um banco de dados interno e nenhum gerenciador de esquema foi detectado. 
# Caso contr�rio, o padr�o � "nenhum".
spring.jpa.hibernate.ddl-auto=update
# Se deseja ativar o registro de instru��es SQL.
spring.jpa.show-sql=true

# Hibernate propriedades nativas adicionais para definir no provedor JPA.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.current_session_context_class=thread
spring.jpa.properties.hibernate.format_sql=true

# Corre��o do erro JPA do Postgres (o m�todo org.postgresql.jdbc.PgConnection.createClob () ainda n�o est� implementado).
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true











